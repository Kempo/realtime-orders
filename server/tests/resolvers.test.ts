describe('resolver tests', () => {
  describe('create checkout session', () => {

    it.skip('should successfully create checkout session');

    it.skip('should throw error if no line items provided', () => {

    });

    it.skip('throw error if unable to parse line items', () => {
      
    });

    it.skip('should throw error if unable to create checkout session', () => {

    });
  });

  describe('fetch order details', () => {
    it.skip('should throw error if no session id provided', () => {

    });

    it.skip('should throw error if invalid session id provided', () => {

    });
  });
});